---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
modules:
  - from-file: common.yml
  - from-file: flatpaks-noxfce.yml
        
  - type: dnf
    install:
      packages:
        - plasma-discover
        
  - type: default-flatpaks
    configurations:
      - notify: false
        scope: system
        repo:
          title: Flathub (system)
        install:
          - org.kde.gwenview
          - org.kde.kcalc
          - org.kde.haruna
          - io.github.DenysMb.Kontainer          
