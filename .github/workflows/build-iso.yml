name: Build ISO
on:
  workflow_run:
    workflows: ["Build Image"]
    types: [completed]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  generate-iso-matrix:
    name: Generate ISO Build Matrix
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Provision Recipes for Runner
        shell: bash
        run: bash ./files/scripts/generate-recipe.sh

      - name: Parse Recipe Files for ISOs
        id: set-matrix
        shell: bash
        run: |
          cd recipes
          JSON_DATA=$(find variants -maxdepth 1 -name "*.yml" -type f | jq -R . | jq -cs '
            map({
              recipe: .,
              filename: (split("/")[-1] | split(".yml")[0])
            })
          ')
          echo "matrix=$JSON_DATA" >> $GITHUB_OUTPUT

  build:
    name: Build ISO
    needs: generate-iso-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.generate-iso-matrix.outputs.matrix) }}
    permissions:
      contents: read
      packages: read
      id-token: write
    steps:
      - name: Maximize build space
        uses: jlumbroso/free-disk-space@main
        with:    
          tool-cache: true

      - name: Checkout
        uses: actions/checkout@main

      - name: Generate Metadata
        id: meta
        shell: bash
        run: |
          FILENAME="${{ matrix.filename }}"
          
          if [[ "$FILENAME" == *-beta ]]; then
            ISO_NAME="${FILENAME%-beta}"
            IMAGE_TAG="beta"
          else
            ISO_NAME="$FILENAME"
            IMAGE_TAG="latest"
          fi

          echo "container_image=$ISO_NAME" >> $GITHUB_OUTPUT
          echo "full_iso_name=$FILENAME" >> $GITHUB_OUTPUT
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "date=$(TZ="Asia/Beirut" date +'%Y%m%d')" >> $GITHUB_OUTPUT
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Fetch Fedora Release
        id: clean_version
        shell: bash
        run: |
          MAX_RETRIES=5
          RETRY_COUNT=0
          SUCCESS=false
          URL="https://fedoraproject.org/releases.json"

          until [ $RETRY_COUNT -ge $MAX_RETRIES ] || [ "$SUCCESS" = true ]; do
            RESPONSE=$(curl -fsSL "$URL" || echo "failed")

            if [ "$RESPONSE" != "failed" ]; then
              VERSION=$(echo "$RESPONSE" | grep -oE '"version": "[0-9]+"' | grep -oE '[0-9]+' | sort -rn | head -n 1)
              if [ -n "$VERSION" ]; then
                echo "stable_one=$VERSION" >> $GITHUB_OUTPUT
                SUCCESS=true
              fi
            else
              sleep 10
              RETRY_COUNT=$((RETRY_COUNT + 1))
            fi
          done

          if [ "$SUCCESS" = false ]; then
            exit 1
          fi

      - name: Resolve Dynamic Flatpak Refs
        id: flatpak_list
        shell: bash
        run: |
          BASE_DIR="files/flatpaks"
          REFS_STRING=""
          EXCLUDE_STRING=""

          process_file() {
            local file_path=$1
            local mode=$2
            if [ -f "$file_path" ]; then
              while IFS= read -r ID || [ -n "$ID" ]; do
                [[ -z "$ID" || "$ID" =~ ^# ]] && continue
                CLEAN_ID=$(echo "$ID" | tr -d '\r' | xargs)
                
                if [[ "$mode" == "exclude" ]]; then
                  EXCLUDE_STRING+="$CLEAN_ID "
                else
                  FULL_REF=$(curl -s "https://flathub.org/api/v2/appstream/$CLEAN_ID" | jq -r '.bundle.value // empty')
                  if [ -n "$FULL_REF" ] && [ "$FULL_REF" != "null" ]; then
                    REFS_STRING+="$FULL_REF "
                  else
                    REFS_STRING+="app/$CLEAN_ID/x86_64/stable "
                  fi
                fi
              done < "$file_path"
            fi
          }

          process_file "$BASE_DIR/app" "include"
          process_file "$BASE_DIR/runtime" "include"

          for item_path in "$BASE_DIR"/*; do
            if [ -f "$item_path" ]; then
              ITEM_NAME=$(basename "$item_path")
              
              if [[ "$ITEM_NAME" == "app" || "$ITEM_NAME" == "runtime" ]]; then
                continue
              fi
              
              if [[ "$ITEM_NAME" == -* ]]; then
                CLEAN_MATCH="${ITEM_NAME#-}"
                IFS=',' read -ra TARGETS <<< "$CLEAN_MATCH"
                for target in "${TARGETS[@]}"; do
                  if [[ "${{ matrix.filename }}" == *"$target"* ]]; then
                    process_file "$item_path" "exclude"
                    break
                  fi
                done
              else
                IFS=',' read -ra TARGETS <<< "$ITEM_NAME"
                for target in "${TARGETS[@]}"; do
                  if [[ "${{ matrix.filename }}" == *"$target"* ]]; then
                    process_file "$item_path" "include"
                    break
                  fi
                done
              fi
            fi
          done

          FINAL_REFS=""
          for ref in $REFS_STRING; do
            MATCHED=false
            for skip in $EXCLUDE_STRING; do
              if [[ "$ref" == *"$skip"* ]]; then
                MATCHED=true
                break
              fi
            done
            if [[ "$MATCHED" == false ]]; then
              FINAL_REFS+="$ref "
            fi
          done

          FINAL_REFS=$(echo "$FINAL_REFS" | xargs)
          echo "refs=$FINAL_REFS" >> $GITHUB_OUTPUT

      - name: Build ISO (First Attempt)
        id: iso_build_1
        continue-on-error: true
        uses: jasonn3/build-container-installer@main
        with:
          arch: x86_64
          image_name: ${{ steps.meta.outputs.container_image }}
          image_repo: ghcr.io/shadowos-linux 
          image_tag: ${{ steps.meta.outputs.image_tag }}
          version: ${{ steps.clean_version.outputs.stable_one }}
          variant: 'Kinoite'
          additional_templates: '/github/workspace/files/installer/remove_root_password_prompt.tmpl'
          flatpak_remote_refs: ${{ steps.flatpak_list.outputs.refs }}
          iso_name: ${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.iso
          enable_cache_dnf: "false"
          enable_cache_skopeo: "false"

      - name: Clean cache after Attempt 1 failure
        if: steps.iso_build_1.outcome == 'failure'
        shell: bash
        run: sudo rm -rf /cache

      - name: Build ISO (Second Attempt)
        id: iso_build_2
        if: steps.iso_build_1.outcome == 'failure'
        continue-on-error: true
        uses: jasonn3/build-container-installer@main
        with:
          arch: x86_64
          image_name: ${{ steps.meta.outputs.container_image }}
          image_repo: ghcr.io/shadowos-linux 
          image_tag: ${{ steps.meta.outputs.image_tag }}
          version: ${{ steps.clean_version.outputs.stable_one }}
          variant: 'Kinoite'
          additional_templates: '/github/workspace/files/installer/remove_root_password_prompt.tmpl'
          flatpak_remote_refs: ${{ steps.flatpak_list.outputs.refs }}
          iso_name: ${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.iso
          enable_cache_dnf: "false"
          enable_cache_skopeo: "false"

      - name: Clean cache after Attempt 2 failure
        if: steps.iso_build_2.outcome == 'failure'
        shell: bash
        run: sudo rm -rf /cache

      - name: Build ISO (Third Attempt)
        id: iso_build_3
        if: steps.iso_build_2.outcome == 'failure'
        uses: jasonn3/build-container-installer@main
        with:
          arch: x86_64
          image_name: ${{ steps.meta.outputs.container_image }}
          image_repo: ghcr.io/shadowos-linux 
          image_tag: ${{ steps.meta.outputs.image_tag }}
          version: ${{ steps.clean_version.outputs.stable_one }}
          variant: 'Kinoite'
          additional_templates: '/github/workspace/files/installer/remove_root_password_prompt.tmpl'
          flatpak_remote_refs: ${{ steps.flatpak_list.outputs.refs }}
          iso_name: ${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.iso
          enable_cache_dnf: "false"
          enable_cache_skopeo: "false"

      - name: Set ISO Output Path Metadata
        id: final_path
        shell: bash
        run: |
          if [ "${{ steps.iso_build_1.outcome }}" == "success" ]; then
            echo "iso_path=${{ steps.iso_build_1.outputs.iso_path }}" >> $GITHUB_OUTPUT
          elif [ "${{ steps.iso_build_2.outcome }}" == "success" ]; then
            echo "iso_path=${{ steps.iso_build_2.outputs.iso_path }}" >> $GITHUB_OUTPUT
          else
            echo "iso_path=${{ steps.iso_build_3.outputs.iso_path }}" >> $GITHUB_OUTPUT
          fi
          
      - name: Upload ISO to Job Artifacts
        uses: actions/upload-artifact@main
        with:
          path: |
            ${{ steps.final_path.outputs.iso_path }}/${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.iso
            ${{ steps.final_path.outputs.iso_path }}/${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.iso-CHECKSUM
          name: ${{ steps.meta.outputs.full_iso_name }}-f${{ steps.clean_version.outputs.stable_one }}-${{ steps.meta.outputs.date }}-${{ steps.meta.outputs.sha_short }}.zip
          retention-days: 4
          compression-level: 0
